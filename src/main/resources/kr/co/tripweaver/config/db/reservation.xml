<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reservationSQL">


	<!-- reservation insert -->
	<insert id="insertReser" parameterType="reservationVo">
		insert into reservation 
		(reser_id,reser_date,reser_apply_peo_count,mem_id,guideplan_id)
		 values 'like'||lpad(res_seq.nextval, 5, 0),sysdate,#{reser_apply_peo_count},#{mem_id},#{guideplan_id}	
	</insert>
	
	<!-- 로그인 한 회원의 예약 조회 -->
	<select id="selectReserByMemId" parameterType="reservationVo">
		select * from reservation where mem_id = #{mem_id}
	</select>
	
	<!--  -->
	<select id="selectReserForGuide">
		select * from reservation where mem_id = #{mem_id}
	</select>
</mapper>