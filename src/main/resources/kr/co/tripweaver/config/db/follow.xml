<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="followSQL">
	
	<select id="selectFollowingCount" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM follow
		WHERE mem_id2 = #{mem_id}
	</select>
  
  	<select id="selectFollowing" parameterType="String" resultType="memberVo">
		SELECT m.*
		FROM member m INNER JOIN follow f
		ON (m.mem_id = f.mem_id)
		AND (f.mem_id2 = #{mem_id})
	</select>
	
	<select id="selectFollowerCount" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM follow
		WHERE mem_id = #{mem_id}
	</select>
	
	<select id="selectFollower" parameterType="String" resultType="memberVo">
		SELECT m.*
		FROM member m INNER JOIN follow f
		ON (m.mem_id = f.mem_id2)
		AND (f.mem_id = #{mem_id})
	</select>
</mapper>